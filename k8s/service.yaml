apiVersion: v1
kind: Service
metadata:
  name: vulnerable-flask-service
spec:
  type: NodePort
  selector:
    app: vulnerable-flask-app
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30080
